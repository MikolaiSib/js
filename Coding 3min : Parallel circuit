function resistance(arr){
  let sum = 0
  for(let i = 0; i < arr.length; i++){
    sum += 1/arr[i].reduce((acc, el) => acc + el, 0)
  }
  return Math.round(1 / sum * 100)/100
  //coding and coding...
}
