var Calculator = {
 average: function() {
   const arr = [...arguments]
   if(arr.length === 0){
     return 0
   }
   const sum =  arr.reduce((acc, el) => acc + el)
   return sum/arr.length
  // Your code here... 
 }
};
