function median(array) {
  let sort = [...array].sort((a, b) => a -b)
  if(array.length%2 === 1){
    return sort[Math.floor(array.length/2)]
  }
  return (sort[Math.floor(array.length/2)-1] + sort[Math.floor(array.length/2)])/2
//code here
}
