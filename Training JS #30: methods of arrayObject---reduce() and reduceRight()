function tailAndHead(arr){
  let prod = 1
  for(let i = 0; i < arr.length - 1; i++){
    let tmpNum = Number(arr[i].toString().slice(arr[i].toString().length - 1)) + Number(arr[i+1].toString().slice(0, 1))
    prod *= tmpNum
  }
  return prod
  //coding here...
}
