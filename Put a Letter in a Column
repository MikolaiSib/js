function buildRowText(index, character) {
  let range = [1, 3, 5, 7, 9, 11, 13, 15, 17]
  let str = '| | | | | | | | | |'
  return str.slice(0, range[index]) + character + str.slice(range[index]+1)
  // Do your magic :)
}
