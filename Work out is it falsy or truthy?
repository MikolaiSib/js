function falsyOrTruthy(arr) {
  let arrFalse = []
  let arrTrue = []
  for(let i = 0; i < arr.length; i++){
    if(arr[i]){
      arrTrue.push(arr[i])
    } else {
      arrFalse.push(arr[i])
    }
  }
  return arr.length%2 ? arrFalse : arrTrue
//your code...

}
