function stantonMeasure(a){
  let count1 = 0
  for(let i = 0; i < a.length; i++){
    if(a[i] === 1){
       count1 += 1
    }
  }
  let count = 0
  for(let i = 0; i < a.length; i++){
    if(a[i] === count1){
       count += 1
    }
  }
  return count
}
