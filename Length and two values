function alternate(n, firstValue, secondValue){
  let resArr = []
  for(let i = 0; i<n; i++){
    if(i%2){
      resArr.push(secondValue)
    } else {
      resArr.push(firstValue)
    }
  }
  return resArr
  // your code
}
