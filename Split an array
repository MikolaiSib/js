function split(arr, prop) {
  let trueArr = []
  let falseArr = []
  for(let i = 0; i < arr.length; i++){
    if(arr[i][prop]){
      trueArr.push(arr[i])
    } else {
      falseArr.push(arr[i])
    }
  }
  return [trueArr, falseArr]
}
