function minimumSteps(numbers, value){
  numbers.sort((a, b) => a -b)
  let sum = 0
  for(let i = 0; i < numbers.length; i++){
    sum += numbers[i]
    if(sum >= value){
      return i 
    }
  }
  //your code here
}
